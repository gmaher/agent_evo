{
  "tools": [
    {
      "id": "file_reader",
      "name": "read_file",
      "description": "Read the contents of a file",
      "parameters": [
        {
          "name": "file_path",
          "type": "string",
          "description": "Path to the file to read",
          "required": true
        }
      ],
      "returns": {
        "type": "string",
        "description": "Contents of the file"
      },
      "code": "def read_file(file_path):\n    try:\n        with open(file_path, 'r') as file:\n            return file.read()\n    except FileNotFoundError:\n        return 'File not found'\n    except Exception as e:\n        return str(e)"
    },
    {
      "id": "file_writer",
      "name": "write_file",
      "description": "Write content to a file",
      "parameters": [
        {
          "name": "file_path",
          "type": "string",
          "description": "Path to the file to write",
          "required": true
        },
        {
          "name": "content",
          "type": "string",
          "description": "Content to write to the file",
          "required": true
        },
        {
          "name": "mode",
          "type": "string",
          "description": "Mode for writing the file, e.g., 'w' for write, 'a' for append",
          "required": false,
          "default": "w"
        }
      ],
      "returns": {
        "type": "string",
        "description": "Confirmation message or error"
      },
      "code": "def write_file(file_path, content, mode='w'):\n    try:\n        with open(file_path, mode) as file:\n            file.write(content)\n        return 'Write successful'\n    except Exception as e:\n        return str(e)"
    },
    {
      "id": "code_analyzer",
      "name": "analyze_python_code",
      "description": "Analyze Python code for bugs and suggest fixes",
      "parameters": [
        {
          "name": "code",
          "type": "string",
          "description": "Python code to analyze",
          "required": true
        }
      ],
      "returns": {
        "type": "object",
        "description": "Analysis results with suggestions for fixing bugs"
      },
      "code": "def analyze_python_code(code):\n    # Placeholder for Python code analysis logic\n    return {'valid': True, 'suggestions': []}"
    }
  ]
}