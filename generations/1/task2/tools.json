{
  "tools": [
    {
      "id": "file_reader",
      "name": "read_file",
      "description": "Read content from a file with proper error handling.",
      "parameters": [
        {
          "name": "file_path",
          "type": "string",
          "description": "Path to the file to be read.",
          "required": true
        },
        {
          "name": "encoding",
          "type": "string",
          "description": "File encoding type.",
          "required": false,
          "default": "utf-8"
        }
      ],
      "returns": {
        "type": "string",
        "description": "Content of the file or error message."
      },
      "code": "def read_file(file_path, encoding='utf-8'):\n    try:\n        with open(file_path, 'r', encoding=encoding) as file:\n            return file.read()\n    except Exception as e:\n        return f'Error reading file: {str(e)}'"
    },
    {
      "id": "file_writer",
      "name": "write_file",
      "description": "Write content to a file with proper error handling.",
      "parameters": [
        {
          "name": "file_path",
          "type": "string",
          "description": "Path to the file to be written.",
          "required": true
        },
        {
          "name": "content",
          "type": "string",
          "description": "Content to write to the file.",
          "required": true
        },
        {
          "name": "mode",
          "type": "string",
          "description": "File writing mode, 'w' for overwrite, 'a' for append.",
          "required": false,
          "default": "w"
        },
        {
          "name": "encoding",
          "type": "string",
          "description": "File encoding type.",
          "required": false,
          "default": "utf-8"
        }
      ],
      "returns": {
        "type": "string",
        "description": "Success message or error."
      },
      "code": "def write_file(file_path, content, mode='w', encoding='utf-8'):\n    try:\n        with open(file_path, mode, encoding=encoding) as file:\n            file.write(content)\n        return f'Successfully wrote to {file_path}'\n    except Exception as e:\n        return f'Error writing file: {str(e)}'"
    },
    {
      "id": "web_search",
      "name": "perform_web_search",
      "description": "Search the web and return structured data.",
      "parameters": [
        {
          "name": "query",
          "type": "string",
          "description": "Search query.",
          "required": true
        },
        {
          "name": "limit",
          "type": "int",
          "description": "Maximum number of results to return.",
          "required": false,
          "default": 5
        }
      ],
      "returns": {
        "type": "list",
        "description": "List of search results with title and link."
      },
      "code": "def perform_web_search(query, limit=5):\n    import requests\n    try:\n        response = requests.get(f'https://api.example.com/search?q={query}&limit={limit}')\n        response.raise_for_status()\n        return response.json()\n    except Exception as e:\n        return f'Error performing web search: {str(e)}'"
    },
    {
      "id": "data_analyzer",
      "name": "analyze_data",
      "description": "Analyze and synthesize information from raw data.",
      "parameters": [
        {
          "name": "data",
          "type": "string",
          "description": "Raw data to analyze.",
          "required": true
        }
      ],
      "returns": {
        "type": "string",
        "description": "Synthesized analysis or summary."
      },
      "code": "def analyze_data(data):\n    try:\n        # Placeholder for data analysis logic\n        return f'Analyzed data: {data[:100]}...'\n    except Exception as e:\n        return f'Error analyzing data: {str(e)}'"
    }
  ]
}